# Minotaur Validator - .env example (copy to .env and adjust)

# ============================================
# Bittensor configuration
# ============================================
NETUID=112
WALLET_NAME=my-validator
WALLET_HOTKEY=my-hotkey
SUBTENSOR_NETWORK=finney  # Options: finney, test, local
# SUBTENSOR_ADDRESS=wss://entrypoint-finney.opentensor.ai:443

# ============================================
# Aggregator API configuration
# ============================================
AGGREGATOR_URL=http://127.0.0.1:4100
VALIDATOR_API_KEY=your-validator-api-key  # REQUIRED: Validator-specific API key for validator endpoints
AGGREGATOR_TIMEOUT=10
AGGREGATOR_VERIFY_SSL=1
AGGREGATOR_MAX_RETRIES=3
AGGREGATOR_BACKOFF_SECONDS=0.5
AGGREGATOR_PAGE_LIMIT=500

# ============================================
# Loop / tempo (optional overrides)
# ============================================
VALIDATOR_POLL_SECONDS=12
VALIDATOR_FINALIZATION_BUFFER_BLOCKS=6
VALIDATOR_BACKOFF_FACTOR=2.0
VALIDATOR_BACKOFF_MAX_SECONDS=120

# ============================================
# Burn allocation (optional)
# ============================================
# BURN_PERCENTAGE=0.95  # Allocate 95% to creator hotkey for burning, 5% to miners

# ============================================
# Validator mode
# ============================================
# VALIDATOR_MODE=bittensor      # bittensor (production) or mock (testing)

# ============================================
# Simulator settings (for order validation)
# ============================================
# SIMULATOR_RPC_URL=https://mainnet.infura.io/v3/YOUR_INFURA_KEY
# SIMULATOR_DOCKER_IMAGE=mino-simulation
# SIMULATOR_MAX_CONCURRENT=5  # Maximum number of concurrent simulations (default: 5)
# SIMULATOR_TIMEOUT_SECONDS=300  # Simulation timeout in seconds (default: 300 = 5 minutes)

# ============================================
# Ethereum RPC (for solver price quotes)
# ============================================
# ETHEREUM_RPC_URL=https://mainnet.infura.io/v3/YOUR_INFURA_KEY
# Alternative: Use ALCHEMY_API_KEY to auto-construct Alchemy URL
# ALCHEMY_API_KEY=your-alchemy-api-key

# ============================================
# Validator identity
# ============================================
# VALIDATOR_ID=your-validator-id  # Unique ID for order filtering (defaults to hotkey)

# ============================================
# Epoch mode (optional - for order-based validation)
# ============================================
# VALIDATOR_EPOCH_MINUTES=5     # Run in epoch mode with 5-minute epochs
# VALIDATOR_CONTINUOUS=true     # Enable continuous epoch-based validation

# ============================================
# Miner configuration
# ============================================
# MINER_MODE=simulation  # simulation or bittensor
# MINER_ID=my-test-miner  # Miner identifier (required in simulation mode)
# MINER_API_KEY=your-miner-api-key  # REQUIRED: Miner API key for /v1/solvers/* endpoints
# MINER_BASE_PORT=8000  # Base port for solver servers (each solver uses base_port + index)
# MINER_SOLVER_HOST=localhost  # Host address for solver endpoint (use host.docker.internal or host IP if aggregator is in Docker)
# MINER_NUM_SOLVERS=1  # Number of solvers to run (default: 1)

# ============================================
# Simulation mode settings (real aggregator, no Bittensor)
# ============================================
# AGGREGATOR_URL=http://127.0.0.1:4100      # Aggregator API endpoint
# AGGREGATOR_API_KEY=your-aggregator-api-key  # Bearer token (will be required soon)
# SIMULATOR_RPC_URL=https://mainnet.infura.io/v3/YOUR_INFURA_KEY
# SIMULATOR_DOCKER_IMAGE=mino-simulation

# ============================================
# Logging (optional)
# ============================================
# LOGURU_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR


