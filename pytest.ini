[pytest]
filterwarnings =
    ignore::DeprecationWarning:websockets\.legacy(\..*)?
markers =
    cross_chain: dev-track cross-chain / multi-leg tests. Not run by default; invoke with `pytest -m cross_chain` or `make test-cross-chain`.
